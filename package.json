{
  "name": "auto-continue",
  "displayName": "🤖 Авто Продолжение Чата",
  "description": "Простое расширение для автоматического продолжения диалога с ИИ",
  "version": "0.0.2",
  "publisher": "your-name",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "chat",
    "ai",
    "automation",
    "continue",
    "диалог"
  ],
  "activationEvents": [
    "onView:autoContinueView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "autoContinue.start",
        "title": "🚀 Запустить Авто Продолжение",
        "icon": "$(play)"
      },
      {
        "command": "autoContinue.stop",
        "title": "🛑 Остановить Авто Продолжение",
        "icon": "$(stop)"
      },
      {
        "command": "autoContinue.toggle",
        "title": "🔄 Переключить Авто Продолжение",
        "icon": "$(play-circle)"
      }
    ],
    "views": {
      "autoContinueContainer": [
        {
          "id": "autoContinueView",
          "name": "🎮 Панель Управления",
          "when": "true"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "autoContinueContainer",
          "title": "🤖 Авто Продолжение",
          "icon": "$(robot)"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "autoContinue.toggle",
          "when": "view == autoContinueView",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "autoContinue.start",
          "when": "view == autoContinueView && viewItem == stopped",
          "group": "control"
        },
        {
          "command": "autoContinue.stop",
          "when": "view == autoContinueView && viewItem == running",
          "group": "control"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  }
}
